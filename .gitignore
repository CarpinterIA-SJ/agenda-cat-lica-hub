# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright
/playwright-report/
/test-results/
/playwright/.cache/

# Claude Code local config / Supabase CLI temp
.claude/
supabase/.temp/

# Skills de agente instaladas localmente (ferramental, nao faz parte do
# produto). .agents/ sao 4474 arquivos vindos de um repo publico de
# terceiro, reproduziveis a partir do lock — mesma categoria de
# node_modules. skills-lock.json e o lock desse ferramental pessoal, nao
# uma dependencia do build nem do CI.
.agents/
skills-lock.json

# Environment variables with secrets
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
